@namespace("foo.bar")
protocol NestedRecords {
  record UserName {
    string value;
  }
  record Password {
    string hash;
    string salt;
  }
  record LoginCredentials {
    UserName userName;
    Password password;
  }
}